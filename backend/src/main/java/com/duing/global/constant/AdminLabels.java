package com.duing.global.constant;

/**
 * 어드민 응답 전반에 쓰이는 공통 라벨.
 * 삭제·소실된 참조 엔티티(클럽, 사용자, 라운드 등) 자리에 사용한다.
 */
public final class AdminLabels {

    public static final String DELETED = "(삭제됨)";

    private AdminLabels() {
        // 상수 모음 클래스 — 인스턴스화 금지
    }
}
