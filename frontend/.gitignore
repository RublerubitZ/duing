node_modules
.pnpm-store
.pnpm-debug.log*

# build outputs
.next
out
dist
build
*.tsbuildinfo

.vercel
.env*

# env
.env
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# IDE / OS
.idea
.vscode
.DS_Store

# test / coverage
coverage
.nyc_output

# OpenAPI codegen 산출물 — `pnpm gen:api` 로 언제든 다시 만든다(live 백엔드 필요).
# 커밋해 두면 재생성 없이 늙어 실제 응답과 어긋난 채 남는다.
packages/api/src/generated/

# misc
*.log
.turbo

# Playwright (실행 산출물 — 실패 스크린샷·트레이스)
test-results/
playwright-report/